from infrahub_sdk.generator import InfrahubGenerator


class CampusSiteGenerator(InfrahubGenerator):
    """Provision a campus site's devices from its assigned design.

    You implement generate() section by section in Challenge 3 — each step of
    the guide replaces exactly one of the TODO comments below.
    """

    async def generate(self, data: dict) -> None:
        # TODO (Step 03): Extract the site data

        # TODO (Step 04): Allocate the management prefix and BGP ASN

        # TODO (Step 05): Create the IP pool

        # TODO (Step 06): Create the devices
        pass
