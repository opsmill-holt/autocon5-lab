---
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: CoreGeneratorGroup
  data:
    - name: campus_sites
      description: LocationSite instances managed by the campus site generator

---
apiVersion: infrahub.app/v1
kind: Object
spec:
  kind: CoreStandardGroup
  data:
    - name: campus_routers
      description: Border router devices in campus sites — generator group membership

    - name: all_campus_devices
      description: All devices in campus sites — artifact generation targets
